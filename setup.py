from setuptools import setup, find_packages

setup(
    name="scan_scn",
    version="1.0",
    description="SCN feature identification and characterization for bulk and single-cell RNA-seq",
    author="Favour Esedebe",
    packages=find_packages(),
    include_package_data=True
)
